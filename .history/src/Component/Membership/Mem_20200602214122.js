import React from 'react';
import { useParams } from 'react-router-dom';
const Mem = () => {
    return (
        const { plan }=useParams();
        <div>
            
        </div>
    );
};

export default Mem;