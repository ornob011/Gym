import React from 'react';
import { useParams } from 'react-router-dom';
const Mem = () => {
    return (
        <div>
            const { plan }=useParams();
        </div>
    );
};

export default Mem;